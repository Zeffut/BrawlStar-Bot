# Bot worker dependencies. Pins are loose to let pip resolve
# conflicts (scrcpy-client and adbutils share a transitive dep).

# Vision + OCR
easyocr>=1.7,<2
opencv-python>=4.8,<5
numpy>=1.26,<2
Pillow>=10

# ONNX inference + supporting models
onnxruntime>=1.18,<2
ultralytics>=8.3,<9

# Device control
# scrcpy-client + av must be installed manually due to dependency
# conflicts (scrcpy-client pins an old `av` that needs Cython on Windows
# py312). Install them with:
#   pip install av adbutils
#   pip install scrcpy-client --no-deps
adbutils

# Networking + APIs
requests>=2.32
aiohttp>=3.10

# Web panel (local + cloud sync)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10

# Misc
packaging>=24
toml>=0.10
google-play-scraper>=1.2
discord-webhook>=1.3
