# Bot worker dependencies (run on the host machine that drives the
# emulator / phone). The cloud panel has its own requirements.txt
# inside cloud_panel/.

# Vision + OCR
easyocr==1.7.2
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0

# ONNX inference + supporting models
onnxruntime==1.20.1
ultralytics==8.3.0

# Device control
scrcpy-client==0.4.1
adbutils==2.8.0

# Networking + APIs
requests==2.32.3
aiohttp==3.11.10

# Web panel (local + cloud sync)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5

# Misc
packaging==24.2
toml==0.10.2
google-play-scraper==1.2.7
discord-webhook==1.3.1
