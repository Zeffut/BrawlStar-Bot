# Install in this order to avoid scrcpy-client / adbutils version conflict.
# Use `./install.sh` instead of `pip install -r` for the right sequencing.
numpy<2.0.0
opencv-python>=4.8.0
onnxruntime>=1.20.0
ultralytics>=8.4.0
adbutils==2.12.0
av==12.3.0
Pillow>=10.0.0
rich>=13.0.0
pytest>=8.0.0
# scrcpy-client must be installed AFTER the above with --no-deps:
#   pip install --no-deps "git+https://github.com/leng-yue/py-scrcpy-client.git@v0.5.0"
