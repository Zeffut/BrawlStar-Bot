# Bot worker dependencies. Pins are loose to let pip resolve
# conflicts (scrcpy-client and adbutils share a transitive dep).

# Vision + OCR
easyocr>=1.7,<2
opencv-python>=4.8,<5
numpy>=1.26,<2
Pillow>=10

# ONNX inference + supporting models
onnxruntime>=1.18,<2
ultralytics>=8.3,<9

# Device control
# av: pin to a version with Windows py312 wheels (scrcpy-client otherwise
# pulls a too-old av that needs Cython + VC build tools).
av>=12.0
scrcpy-client>=0.4
adbutils

# Networking + APIs
requests>=2.32
aiohttp>=3.10

# Web panel (local + cloud sync)
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.10

# Misc
packaging>=24
toml>=0.10
google-play-scraper>=1.2
discord-webhook>=1.3
